// import express, { Router } from 'express';
// import authenticateJWT from '../middleware/auth.middleware';
// import {createWallet, deposit, transfer, withdraw, getBalance, getTransactionHistory } from '../controllers/wallet.controller';

// const router: Router = express.Router();

// router.post('/create', authenticateJWT, createWallet);
// router.post('/deposit', authenticateJWT, deposit);
// router.post('/transfer', authenticateJWT, transfer);
// router.post('/withdraw', authenticateJWT, withdraw);
// router.get('/balance', authenticateJWT, getBalance);
// router.get('/transactions', authenticateJWT, getTransactionHistory);

// export default router;